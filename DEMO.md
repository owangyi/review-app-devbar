# 🎯 Demo Guide - 后端切换演示指南

## 演示场景说明

**前端分支不变，切换后端分支，前端数据会实时变化**。
[!image]https://www.notion.so/image/attachment%3A6dde3a0b-31bc-4668-bb04-d9562fb39256%3Aimage.png?table=block&id=2b938ea9-ba76-8040-94a7-e1a05b2aba94&spaceId=82b8862c-d3ff-414b-8f5c-9225193c0611&width=2000&userId=e8d6d791-95b7-49d0-9998-1448444c02be&cache=v2

这样可以：
1. 🔄 在同一个前端界面测试不同版本的后端API
2. 🧪 快速验证后端改动是否符合预期
3. 🎭 演示不同后端分支的数据差异

## 📊 数据差异设计

### 不同后端分支的数据特征

| 后端分支 | 用户数量 | 用户名特征 | 总用户数 | 总收入 | 增长率 |
|---------|---------|-----------|---------|--------|--------|
| `develop` | 5人 | xxx (Dev) | ~1000 | ~35K | ~8.5% |
| `feature-dev-001` | 7人 | xxx (F001) | ~1500 | ~52K | ~15.3% |
| `feature-dev-002` | 9人 | xxx (F002) | ~2200 | ~78K | ~22.7% |

**关键差异点：**
- ✅ 用户名称不同（包含分支标识）
- ✅ 用户数量不同
- ✅ 统计数据明显不同
- ✅ 每个后端分支独立存储状态

## 🎬 演示步骤

### 场景一：基础后端切换（推荐演示）

**目标：** 展示在同一前端页面，切换后端分支后数据变化

#### Step 1: 访问Feature分支前端
```
http://feature-dev-001.discovery.wang
```

#### Step 2: 查看用户管理Tab
1. 点击 "👥 User Management" Tab
2. 观察显示的用户列表
3. **重点关注：**
   - 页面右上角显示 `📡 Backend: feature-dev-001`
   - 用户名后缀为 `(F001)`
   - 共有 7 个用户

#### Step 3: 切换到develop后端
1. 打开底部 DevBar
2. 在 "Backend" 下拉框选择 `develop`
3. 页面自动重新加载

#### Step 4: 观察数据变化
- ✅ 后端标识变为：`📡 Backend: develop`
- ✅ 用户名变为：xxx (Dev)
- ✅ 用户数量变为：5 个
- ✅ **前端URL未变化**，仍是 `feature-dev-001.discovery.wang`

#### Step 5: 切换到feature-dev-002后端
1. 再次打开 DevBar
2. 选择 `feature-dev-002` 后端
3. 观察变化：
   - 用户名变为：xxx (F002)
   - 用户数量变为：9 个

### 场景二：统计数据对比

**目标：** 展示不同后端的统计数据差异

#### Step 1: 在feature-dev-001前端，develop后端
```
前端：feature-dev-001.discovery.wang
后端：develop (通过DevBar切换)
```

查看统计面板：
- Total Users: ~1000
- Total Revenue: ~35K
- Growth Rate: ~8.5%

#### Step 2: 切换到feature-dev-001后端
```
前端：feature-dev-001.discovery.wang (不变)
后端：feature-dev-001 (通过DevBar切换)
```

查看统计面板：
- Total Users: ~1500 (增加50%)
- Total Revenue: ~52K (增加49%)
- Growth Rate: ~15.3% (增加80%)

#### Step 3: 切换到feature-dev-002后端
```
前端：feature-dev-001.discovery.wang (不变)
后端：feature-dev-002 (通过DevBar切换)
```

查看统计面板：
- Total Users: ~2200 (增加120%)
- Total Revenue: ~78K (增加123%)
- Growth Rate: ~22.7% (增加167%)

### 场景三：跨分支组合（高级演示）

**目标：** 展示任意前后端分支组合都能正常工作

#### 组合1: feature-dev-001 前端 + feature-dev-002 后端
```
访问: http://feature-dev-001.discovery.wang
切换后端: feature-dev-002
```
- ✅ 前端界面来自 feature-dev-001
- ✅ 数据来自 feature-dev-002
- ✅ 显示 9 个用户，名字后缀 (F002)

#### 组合2: feature-dev-002 前端 + develop 后端
```
访问: http://feature-dev-002.discovery.wang
切换后端: develop
```
- ✅ 前端界面来自 feature-dev-002
- ✅ 数据来自 develop
- ✅ 显示 5 个用户，名字后缀 (Dev)

#### 组合3: feature-dev-002 前端 + feature-dev-001 后端
```
访问: http://feature-dev-002.discovery.wang
切换后端: feature-dev-001
```
- ✅ 前端界面来自 feature-dev-002
- ✅ 数据来自 feature-dev-001
- ✅ 显示 7 个用户，名字后缀 (F001)

### 场景四：数据持久化验证

**目标：** 证明不同后端分支的数据独立存储

#### Step 1: 在develop后端修改用户状态
```
前端：任意 feature 分支
后端：develop
```
1. 切换用户1的状态（Active → Inactive）
2. 刷新页面，确认状态保持

#### Step 2: 切换到feature-dev-001后端
```
后端：feature-dev-001
```
- ✅ 用户1状态仍为 Active（未受影响）
- ✅ 证明数据独立存储

#### Step 3: 修改feature-dev-001的用户状态
```
后端：feature-dev-001
```
1. 切换用户1的状态（Active → Inactive）
2. 切换回develop后端
3. 观察develop的用户1仍为Inactive
4. 切换回feature-dev-001
5. 观察feature-dev-001的用户1也为Inactive

#### Step 4: 验证跨分支独立性
```
develop后端：用户1 = Inactive
feature-dev-001后端：用户1 = Inactive
feature-dev-002后端：用户1 = Active (未修改过)
```

## 🎥 演示话术建议

### 开场白
> "这个系统最大的特点是可以在同一个前端界面，动态切换不同版本的后端API。我们来演示一下。"

### 演示过程
> "现在我们在 feature-dev-001 前端分支上，可以看到这里显示的是来自 feature-dev-001 后端的数据，
> 有7个用户，名字后面标注了 (F001)。
> 
> 现在我打开底部的 DevBar，切换后端到 develop 分支...
> 
> 大家看，前端URL没有变化，但是数据完全变了！
> - 用户变成了5个
> - 名字变成了 (Dev) 后缀
> - 统计数据也明显不同
> 
> 这意味着我可以用同一个前端界面，测试不同版本的后端API，
> 非常适合前后端并行开发的场景。"

### 高级演示
> "更厉害的是，我们可以任意组合前后端分支。
> 比如我现在在 feature-dev-001 的前端，但是用的是 feature-dev-002 的后端API。
> 
> 这在实际开发中很有用，比如：
> - 前端团队在开发新UI
> - 后端团队在另一个分支优化API
> - 两个团队可以独立部署，随时组合测试"

### 结束语
> "所有的分支切换都是通过Cookie实现的，不需要重新部署或重启服务。
> Nginx根据域名和Cookie自动路由到对应的版本，实现了真正的动态环境切换。"

## 🧪 快速验证命令

### 测试不同后端返回不同数据

```bash
# 测试develop后端
curl -H "Host: develop.api.discovery.wang" \
     http://localhost/api/users.php | jq '.users[0].name'
# 输出: "Alice Johnson (Dev)"

# 测试feature-dev-001后端
curl -H "Host: feature-dev-001.api.discovery.wang" \
     http://localhost/api/users.php | jq '.users[0].name'
# 输出: "Alex Chen (F001)"

# 测试feature-dev-002后端
curl -H "Host: feature-dev-002.api.discovery.wang" \
     http://localhost/api/users.php | jq '.users[0].name'
# 输出: "Anna Schmidt (F002)"
```

### 对比统计数据

```bash
# Develop后端统计
curl -H "Host: develop.api.discovery.wang" \
     http://localhost/api/statistics.php | jq '.stats | {total_users, total_revenue, growth_rate}'

# Feature-dev-001后端统计
curl -H "Host: feature-dev-001.api.discovery.wang" \
     http://localhost/api/statistics.php | jq '.stats | {total_users, total_revenue, growth_rate}'

# Feature-dev-002后端统计
curl -H "Host: feature-dev-002.api.discovery.wang" \
     http://localhost/api/statistics.php | jq '.stats | {total_users, total_revenue, growth_rate}'
```

## 📸 演示截图检查点

演示时应该能看到以下关键元素：

### ✅ 用户管理页面
- [ ] 页面右上角明显的后端分支标识（紫色渐变背景）
- [ ] 用户数量显示（x users）
- [ ] 用户名包含分支标识后缀
- [ ] DevBar显示当前前后端分支

### ✅ 统计面板页面
- [ ] 后端分支标识（紫色渐变背景）
- [ ] 6个统计卡片数据明显不同
- [ ] Last updated 时间戳
- [ ] 自动刷新功能

### ✅ DevBar组件
- [ ] Frontend下拉框显示所有前端分支
- [ ] Backend下拉框显示所有后端分支
- [ ] 当前选中的分支高亮显示
- [ ] 切换后页面自动重新加载

## 🎓 技术要点说明

### Cookie机制
```
x_target_frontend=feature-dev-001  → 控制前端分支
x_target_backend=develop           → 控制后端分支
```

### Nginx路由
```
前端: *.discovery.wang → /srv/nginx/pages/{branch}/public
后端: *.api.discovery.wang → /srv/nginx/pages/{branch}/api/public
```

### API端点
```
GET  /api/users.php        - 获取用户列表
POST /api/users.php        - 切换用户状态
GET  /api/statistics.php   - 获取统计数据
```

### 数据存储
```
/tmp/users_data_develop.json          - develop分支用户数据
/tmp/users_data_feature-dev-001.json  - feature-dev-001分支用户数据
/tmp/users_data_feature-dev-002.json  - feature-dev-002分支用户数据
```

## 🔧 故障排查

### 问题1: 切换后端后数据没变化
**原因：** 可能Cookie没有设置成功  
**解决：** 检查浏览器控制台，执行 `document.cookie` 查看

### 问题2: 显示"Backend: unknown"
**原因：** API没有返回backend_branch字段  
**解决：** 检查API响应，确保PHP文件已更新

### 问题3: 不同后端数据相同
**原因：** PHP文件可能使用的是旧版本  
**解决：** 确认dist和public目录的PHP文件已更新

## 🎁 额外演示点

### 1. 浏览器开发者工具演示
打开Network面板，展示：
- API请求的Host header
- 响应中的backend_branch字段
- Cookie的自动发送

### 2. 多窗口对比
同时打开两个浏览器窗口：
- 窗口1: feature-dev-001前端 + develop后端
- 窗口2: feature-dev-001前端 + feature-dev-002后端
- 对比显示的数据差异

### 3. 实时刷新演示
- 统计面板会每30秒自动刷新
- 数据有小幅波动（模拟实时数据）
- 但后端分支标识始终不变

## 📝 演示后问答准备

**Q: 前端切换需要重新打包吗？**  
A: 不需要。每个分支的前端已经独立部署，切换只是改变访问的URL。

**Q: 后端切换需要重启服务吗？**  
A: 不需要。所有后端分支也是独立部署的，Nginx根据Host header路由。

**Q: 可以同时运行多少个分支？**  
A: 理论上无限制，只要服务器资源足够。每个分支占用独立的磁盘空间。

**Q: 数据是共享的还是隔离的？**  
A: 完全隔离。每个后端分支有独立的数据文件，互不影响。

**Q: 如何增加新的分支？**  
A: 通过CI/CD自动部署。GitLab检测到新分支push，自动构建并部署。

**Q: 生产环境也用这个吗？**  
A: DevBar主要用于开发和测试。生产环境可以保留组件但隐藏，或完全移除。

## 🚀 下一步

完成演示后，建议查看：
- [FEATURES.md](./FEATURES.md) - 完整功能文档
- [TESTING_GUIDE.md](./TESTING_GUIDE.md) - 测试指南
- [DEPLOYMENT.md](./DEPLOYMENT.md) - 部署说明
- [ARCHITECTURE.md](./ARCHITECTURE.md) - 架构设计

